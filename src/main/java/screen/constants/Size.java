package screen.constants;

import java.awt.*;

public class Size {
    public static int max_window_width = Toolkit.getDefaultToolkit().getScreenSize().width;
    public static int max_window_height = Toolkit.getDefaultToolkit().getScreenSize().height;

    public static int box_size = 150;
}
